Comparison Operator Program in C

This project contains a basic C program that takes two numbers as input and displays simple comparisons between them. It shows whether one number is greater than the other and checks if both numbers are positive.

📌 What the Program Does

Asks the user to enter two integers

Compares the two numbers

Prints which number is greater

Checks if both numbers are positive

Displays simple messages based on the comparison results

▶️ How to Run the Program

Download or clone the repository

Compile the C file using a C compiler, for example:

gcc program.c -o output


Run the program:

./output

🎯 Purpose

This program helps beginners understand:

Basic input/output in C

Simple comparison operations

Using if statements and logical conditions

📜 License

You may use and modify this project freely.

If you'd like, I can also format it in a more stylish or minimal way!
